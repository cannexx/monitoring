package dev.jihun.monitoring.domain.target;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MonitoringTargetRepository extends JpaRepository<MonitoringTarget,String> {
}
